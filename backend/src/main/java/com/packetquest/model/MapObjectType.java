package com.packetquest.model;

/** Kind of decorative / obstruction object placed on the city map. */
public enum MapObjectType {
    DECORATIVE_BUILDING,
    TALL_OBSTRUCTION,
    CONSTRUCTION_ZONE,
    WEATHER_ZONE
}
